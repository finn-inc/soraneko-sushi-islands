# このゲームを生成したプロンプト

空のディレクトリで Claude Code に、以下を1回だけ投げて生成した（途中の人手の介入なし）。

```
# 依頼: お題に沿った3Dフライトゲームを、このディレクトリにゼロから作ってください

## お題
お寿司の島々を飛ぶ猫

## 背景
ゲームにもプログラミングにも詳しくない人の目の前で、この依頼1回だけでゲームが出来上がる様子を見せるデモです。お題はその人がその場で言った一言です。完成したらその人にマウスを渡して遊んでもらいます。
成功の基準は「AIが一発でここまで作るのか」と驚いてもらえること。つまり、動くだけでは足りず、市販のインディーゲームのような「製品に見える」仕上がりが必要です。
途中で私に質問はせず、判断はすべて任せます。検証まで終えてから完了を報告してください。

## 作るもの
ブラウザで動く3Dフライトゲーム。手続き生成された果てのない世界を三人称視点で飛び、空中に並ぶリングをくぐって集める。

### 操作（マウスだけ）
- 機体は常に前進する。マウスカーソルが画面中央からずれている方向へ、なめらかに旋回・上昇・下降する（中央付近は遊びを持たせる）。
- 左ボタンを押している間だけ加速。
- キーボードは使わない。Pointer Lock も使わない（カーソルが消えると初めての人は混乱するため）。
- 初めてマウスを握る人でも酔わず迷子にならないこと: 宙返りはできない角度制限、手を離すと水平に戻る、低すぎ・高すぎは柔らかく押し戻す。

### 失敗状態なし
- 地面・水面・障害物に触れても、ふわっと跳ね返るだけ。ゲームオーバー、ライフ、減点は無し。
- リングは「プレイヤーの現在位置と向きの少し先」に、ゆるやかに蛇行する道筋として生成し続ける。常に次のリングが視界に入り、逃しても罰はなく、道筋は前方に引き直される。難易度調整が不要な作りにする。

### 1回のフライト = 1日
- 朝に飛び立ち、昼、夕焼け、夜へと空と光が変化し、夜が更けたら終了（実時間で約2分）。時間制限を数字ではなく空の色で伝える。夕焼けが見た目の山場になるように。
- 流れ: タイトル画面（背景は自動操縦で飛ぶ実際のゲーム映像）→ クリックで開始 → フライト → リザルト（集めた数・最大コンボ・お題に沿った称号）→ もう一度。

### お題の反映（ここが最重要）
お題を次の7点すべてに翻訳し、一目で「自分の言ったお題だ」と分かるようにする。
1. 空・霧・光・地形の色パレット
2. 地形の性格（島々、山脈、砂丘、雲海など）
3. ランドマーク: お題にちなんだ巨大オブジェクトを、プリミティブの組み合わせで最低5種類作り、世界のあちこちに配置する
4. プレイヤーの機体（お題の主役）
5. リングや収集物の形
6. BGM の雰囲気（テンポ・音色・音階）
7. ゲームタイトルと、リザルトの称号の言葉

### 手触り（省略しないこと）
- 旋回で機体がバンクする。カメラは少し遅れて追従する。
- 加速で視野角が広がり、スピード線や風の粒子が出る。
- リング通過の瞬間: ごく短いヒットストップ、軽い画面揺れ、パーティクルの破裂、リングの発光、そして音。連続でくぐるとコンボになり、音が五音音階で1音ずつ上がっていく（BGM と同じ調にして、何をしても音楽的に聞こえるように）。
- 水面すれすれを飛ぶと水しぶき、など世界が反応する小さな仕掛けを2つ以上。

### 見た目
- フラットシェーディングのローポリで統一。フォグで遠景を溶かし、チャンクの出現を隠す。
- 空のグラデーション、太陽と月、星、水面、雲。余力があればブルーム等のポストプロセス。
- UI は最小限で上品に。日本語。システムフォントのみ。

### 音（すべて Web Audio API で生成）
- BGM（常時ループ、お題の雰囲気、時間帯で少し表情が変わると尚良い）、速度に連動する風の音、リング通過音、コンボ音、リザルトのジングル。
- 最初のクリックで AudioContext を開始。ミュートボタンを用意。

### 説明なしで分かること
- 文章による操作説明は1行まで。開始直後、最初のリングを正面すぐ近くに置き、何もしなくても1個目をくぐれて「こういうゲームか」と分かるようにする。
- 次のリングの方向が分かるさりげないガイドを出す。

## 技術制約
- 画像・音声・3Dモデル・フォントなどの外部アセットは一切使わない。すべてコードで生成する。
- Three.js は使ってよい。npm で取得してプロジェクト内にコピーし、オフラインの会場でも動くこと（実行時の CDN 依存は禁止）。
- ビルド工程なし。index.html と ES Modules の静的ファイル構成で、`python3 -m http.server` のような単純な静的サーバで動くこと。
- 一般的なノート PC の Chrome で 60fps を目標。devicePixelRatio に上限を設け、地形はチャンク単位で生成・破棄する。ウィンドウのリサイズに追従する。

## 検証（完了を報告する前に必ず実施）
デバッグ用に次を実装しておく: `?autopilot=1`（自動操縦でリングを追う。タイトル背景の飛行と共用）、`?timescale=N`（1日の進行を早送り）、`window.__game`（状態・スコア・時刻を読めるオブジェクト）。

1. 静的サーバを起動し、Playwright（無ければインストール）のヘッドレス Chromium で開く。WebGL が有効になる起動フラグを付ける（例: --ignore-gpu-blocklist, --use-angle=swiftshader, --enable-unsafe-swiftshader。環境に合わせて調整）。
2. コンソールエラーとページエラーが 0 件であること。
3. 自動操縦で、タイトル／開始5秒後／昼／夕焼け／夜／リザルトのスクリーンショットを撮り、画像を自分の目で見て確認する: 真っ黒や真っ白でない、地形・空・機体・リングが映っている、UI が重なったりはみ出したりしていない、お題が見た目に反映されている、夕焼けが美しい。
4. 自動操縦でリングを実際に通過でき、スコアとコンボが増えることを `window.__game` で確認する。
5. 早送りで1フライトが最後まで進み、リザルトに到達し、「もう一度」で再開できること。
6. マウス移動とクリックを Playwright で送り、機体の向きと速度が変わることを確認する。
7. 問題があれば直して 1 からやり直す。スクリーンショットを見て「しょぼい」と感じた箇所も直す。

## 完了時の報告
- サーバは起動したままにして URL を伝える。再起動用のコマンドも1行で。
- お題を7点それぞれにどう翻訳したかを短く。
- 検証で見つけて直したこと、直せず残っている既知の問題があれば正直に。
```
